# Binaries
/bb
/dist/
*.exe
*.test
*.out

# Generated by the goreleaser `before` hook (and the local
# Makefile completions target) just before each release. We
# don't want to track the rendered scripts in git — they're
# derived from the cobra command tree and would churn on every
# command-shape change.
/completions/

# Go
vendor/

# Editor
.idea/
.vscode/
.VSCodeCounter/
*.swp

# OS
.DS_Store

# Local config / secrets
*.local.yml
.env
